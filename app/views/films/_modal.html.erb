<div class="modal is-display <%= @is_anime ? "is-anime" : "" %>">
<div class="modal-wrapper">
  <%= link_to root_path, class: "modal-close" do %>
    <img src="<%= asset_path('icon-close.svg') %>" alt="Cosmic Library" width="24" height="24" alt="閉じる" />
  <% end %>
  <%= form_with model: film do |form| %>
    <div class="form">
      <ul class="form-items">
        <li class="form-item">
          <div class="form-chips">
            <div class="form-select form-chips-input"></div>
            <div class="form-chips-options">
              <% @selected_tags ||= [] %>
              <% @tags.each do |tag| %>
                <label class="form-chips-label">
                  <input type="checkbox" name="film_tag_ids[]" value="<%= tag[1] %>" <%= @selected_tags.include?(tag[1]) ? "checked" : "" %>>
                  <span><%= tag[0] %></span>
                </label>
              <% end %>
            </div>
          </div>
        </li>
        <li class="form-item">
          <%= form.text_field :title, class: "form-input", placeholder: "日本語のタイトルを入力してください" %>
        </li>
        <li class="form-item">
          <%= form.text_field :title_en, class: "form-input", placeholder: "英語のタイトルを入力してください" %>
        </li>
        <li class="form-item">
          <%= form.select :review_star, options_for_select([
            ['★☆☆☆☆', 1],
            ['★★☆☆☆', 2],
            ['★★★☆☆', 3],
            ['★★★★☆', 4],
            ['★★★★★', 5]
          ]), {}, class: "form-select text-primary-100" %>
        </li>
        <li class="form-item">
          <%= form.text_area :comment, class: "form-textarea", placeholder: "感想を入力してください" %>
        </li>
      </ul>
      <% if film.errors.any? %>
        <ul class="modal-messages">
          <% film.errors.full_messages.each do |message| %>
            <li class="modal-message"><%= message %></li>
          <% end %>
        </ul>
      <% end %>
      <div class="form-actions">
        <span class="hover-circle">
          <%= link_to "キャンセル", root_path, class: "form-cancel" %>
        </span>
        <span class="hover-circle">
          <%= form.submit "保存", class: "form-submit" %>
        </span>
      </div>
    </div>
  <% end %>
</div>
</div>